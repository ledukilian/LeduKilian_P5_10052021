{% if form_type == 'add' %}
   {% set form_title = 'Ajouter un nouvel article' %}
   {% set article_title = '' %}
   {% set button_label = 'Poster' %}
{% elseif form_type == 'edit' %}
   {% set form_title = 'Modifier un article' %}
   {% set article_title = post.title %}
   {% set article_lead = post.lead %}
   {% set image_alt = post.coverAltImg %}
   {% set article_content = post.content %}
   {% set button_label = 'Modifier' %}
{% endif %}

<div class="container-fluid">
   <div class="row">
      <div class="d-sm-flex align-items-center justify-content-between mb-4">
         <h1 class="h3 mb-0 text-gray-800 mx-2">
            <i class="fas fa-plus-circle mr-2"></i>{{ form_title }}</h1>
      </div>
      <form class="d-flex flex-column col-lg-12 mb-0" action="/admin/blog/ajouter/" method="POST">
         <div class="form-group">
            <label for="titreArticle">Titre</label>
            <input type="text" name="title" class="form-control" id="title" placeholder="Titre de l'article"  required="required" value="{{ article_title }}">
         </div>
         <div class="form-group">
            <label for="titreArticle">Lead Phrase</label>
            <input type="text" class="form-control" name="lead" id="lead" placeholder="Phrase d'entête" required="required" value="{{ article_lead }}">
         </div>
         <div class="form-group d-flex flex-row flex-wrap">
            <label class="col-12 px-0" for="imageArticle">Image</label>
            <input class="col-12 col-md-5" type="file" required="required" name="coverImg" id="coverImg">
            <input type="text" class="form-control col-12 col-md-6" name="coverAltImg" id="coverAltImg" placeholder="Texte alternatif de l'image" required="required" value="{{ image_alt }}">
         </div>
         <div class="form-group">
            <label for="titreArticle">Contenu</label>
            <textarea class="form-control" name="content" id="content" rows="12" required="required" placeholder="Contenu de l'article">{{ article_content }}</textarea>
         </div>

         {% if form_type == 'edit' %}
            <small class="col-12">
               <i class="fas fa-history mr-2"></i>Dernière modification le
               {{ post.updatedat | date('d/m/y à h:i') }}
            </small>
         {% endif %}

         <div class="form-group d-flex flex-row-reverse">
            {% if form_type == 'add' %}
               <button type="submit" class="btn btn-success mx-2">
                  <i class="fas fa-paper-plane mr-2"></i>
                  {{ button_label }}
                  l'article
               </button>
            {% else %}
            <button type="submit" class="btn btn-success mx-2">
               <i class="fas fa-save mr-2"></i>Enregistrer
            </button>
            {% endif %}
         </div>
      </form>
   </div>
</div>